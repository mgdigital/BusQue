build:
    environment:
        php: 7.0.6
        redis: true
    tests:
        override:
            -
                command: 'bin/phpspec run'
                coverage:
                    file: 'coverage.xml'
                    format: 'clover'
checks:
    php: true
filter:
    excluded_paths:
        - features/*
        - spec/*
